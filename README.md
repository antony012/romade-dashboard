# RomaDe Dashboard

Admin Next.js para la API RomaDe.

## Local

```bash
npm install
npm run dev
```

Abre http://localhost:3000

```env
NEXT_PUBLIC_API_URL=http://localhost:3100
```

En producción usa `https://latidos-ve.vercel.app`.

Login: `admin` / `admin123` (el admin por defecto de RomaDe).
